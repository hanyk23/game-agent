# Project Handoff

Updated: 2026-07-19

## Product objective

Develop the reusable Agent that generates, verifies, and packages single-player
vertical bullet-hell H5 games. A game request is evaluation input, not the target.

## Resume protocol

1. Run the recovery protocol in `AGENTS.md`.
2. Read `docs/ROADMAP.md`, `docs/CURRENT_STATUS.md`, Batch 3 Revision 2, and ADRs 0025-0028.
3. Trust files, Git, immutable reports, and newly run tests over this handoff.

## Repository state

- Workspace: `D:\Documents\game agent`
- Git: unborn `master`, no revision; every file is untracked/potentially user-owned.
- Work is uncommitted; no Git change is authorized.

## Roadmap position

- Phase 7 remains active. Batch 1 and Batch 2 Core are complete; required Batch 4 follows Batch 3.
- Batch 3 Revision 2 and ADR 0028 are accepted without weakening ADRs 0026/0027.
- The Manifest 1.4/Assembly 1.3/Graph 1.4 pre-factory ABI gate is complete.
- The complete encounter/hostile-source production factory set is admitted.

## Current Agent capability

- The fixed path and Graph 1.2/1.3 catalog/browser evidence remain passing and immutable.
- All 27 Batch 2 definitions remain loader-admitted; the unchanged graze factory is frozen and tested.
- Strict mixed V1.2/V1.3/V1.4 contexts prevent authority upcast in resolver, isolated, and browser paths.
- Host-minted roots, V3 lineage, independent hostile contention, actor-set combat,
  fractional scoring, and win-first frame-tail outcomes have tested host authority.
- V1.4 production registration, catalog generation, exact grant projection, and
  production instantiation reject missing callables, unresolved services, and evidence drift.
- The cumulative Batch 3 registry starts from the Batch 2 production catalog,
  so it selects the byte-preserved `combat.graze@1.0.0` artifact without
  re-admission or source modification.
- Real condition providers refresh host-retained state before the once-per-frame
  coordinator barrier. Exact outcome ports, internally bound score routes, and
  failure disposal close the production authority boundary and copied runtime.
- Twenty-three Manifest 1.4 encounter/hostile/scoring/outcome factories are loader-admitted:
  scrolling waves, Boss phases, encounter-pattern, fixed/aimed targeting, and
  all eight hostile deliveries at `@1.1`. Actual execution proves enemy slot
  generation reuse, Boss handoff/phases, frozen V3 lineage, distinct pools, and
  two-enemy/Boss contention under one host-owned group.
- Actual scoring/outcome factories route combo source evidence and Boss-defeat
  condition state through host-owned frame-tail win-first commit authority.

## Evaluation case

- Fresh `pnpm check` passes template composition, formatting, both strict
  TypeScript projects, 125 test files / 604 tests, and a 142-module Vite build;
  only the existing >500 kB warning remains.
- Earlier offline run `fa3daaa2-689a-4af0-a7ae-0f2eed511569` is `built`; package SHA-256 is
  `64a98d0559fc0ce004f1779a140e1001caeff273e45aad9b9c3d6eb28aab7ca1`.
- Earlier Edge run `c69c2b21-2942-48b4-8ac7-1642d08c7a09` is `play_checked`; verification
  SHA-256 is `ff4472459740ea202acb16c72658b741e74a4e20c9a57125a086e83533e31691`
  and package SHA-256 is `64d1a393a5474543fd98165191a920045ae0a67c45e499d4bd370b5d420dd722`.
  Desktop/mobile pre-Graze mixed-V1.4 checks pass with zero console/page/request errors.
- No offline/Edge/package run yet covers the current Graze graph changes.

## Constraints and risks

- Preserve fixed/Batch 1/2 evidence; no paid call, credential read, corpus expansion, or Git state change.
- Enemy/Boss authority cannot be simulated by player ownership/assets, shared pools, or local aggregate counters.
- The hostile-source prerequisite for graze is satisfied. A production-registry
  blocked Graph 1.4 test proves the preserved V1.3 reader's exact entity-channel
  and projectile-lineage grants from a V1.4 hostile radial source, without a
  context/authority upcast. Runtime score routing and ready closure remain unproven.
- Full rendered legacy parity, recovery, and packaging remain later Batch 3 gates; do not retire the fixed path.

## Exact next step

Admit actor-set health and source-first projectile-root/root-player contact
factories through the existing host coordinators, then bind their exact graph
routes into the deterministic legacy assembly.
